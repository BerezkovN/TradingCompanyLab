<UserControl x:Class="WpfApp.MVVM.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <TextBox x:Name="UsernameEditBox" HorizontalAlignment="Left" 
                 Margin="270,200,0,0" TextWrapping="Wrap" 
                 Text="Username" VerticalAlignment="Top" 
                 Height="20" Width="120"/>
        
        <TextBox x:Name="PasswordEditBox" HorizontalAlignment="Right" 
                 Margin="0,200,270,0" TextWrapping="Wrap" 
                 Text="Password" VerticalAlignment="Top" 
                 Height="20" Width="120"/>

        <Button x:Name="LoginButton" Content="Login" 
                HorizontalAlignment="Center" Margin="0,242,0,0" 
                VerticalAlignment="Top" Width="92"
                Command="{Binding LoginCommand}"/>
    </Grid>
</UserControl>
